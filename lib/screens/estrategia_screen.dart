import 'package:flutter/material.dart';

class EstrategiaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('EstrategiaScreen'),
      ),
    );
  }
}
